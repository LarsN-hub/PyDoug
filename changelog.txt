v0.7.3-alpha
- Fixed watershed labeling crashing PyDoug on empty image arrays
- Fixed watershed labeling radius causing it to overlook small regions
- Fixed watershed labeling bug with entirely black/white images
- Fixed error with screenshotting label layers without custom colormaps in batch processing
- Changed default opacity of labels and masks
- Changed default directory of file dialog for output directory in batch processing to open one up from the selected parameters directory
- Fixed occasional bug when importing 2D images
- Added data export to domain size distribution plot widget
- Fixed bugs with creating multiple plots and no image operations in batch processing

v0.7.2-alpha
- Fixed obscene width of color bar generated in the "Image to Labels" widget

v0.7.1-alpha
- Fixed bug with saving and importing masks in parameters directory after batch processing with unique masks overhaul

v0.7.0-alpha
- Added ability to create unique masks for each batch processing dataset instead of only using the same mask for all datasets for all widgets using masks
- Overhauled batch processing operation to selectively not overwrite the image file on each operation in order to enable the above without causing memory issues
- Added mask logic widget to batch processing

v0.6.4-alpha
- Added threshold readouts when using histogram-based thresholding (like Otsu)
- Increased font sizes in all plots
- Fixed screenshot capture batch processing bug

v0.6.3-alpha
- Fixed bugs in ridge detection batch processing

v0.6.2-alpha
- Fixed/added proper along axis functionality to edge detection widget

v0.6.1-alpha
- Fixed error where batch processing file selection dialog could not locate external hard drives if importing image sequences

v0.6.0-alpha
- Added functionality to trim widget: with bounds as slices checked, leave max as 0 to go from min to end of axis
- Fixed bug in trim widget where a trimming one slice out of a 3D array would leave a 3D array with one slice instead of a 2D array
- Added resize widget in transform tab with batch processing capability
- Added "Visualize" tab with widgets for converting images to labels and vice versa with colormap gradient control
- Added screenshot capturing batch processing capabilities for complex color mapping and render settings for image and labels layers

v0.5.0-alpha
- Added ridge detection widget to Features tab
- Added blob detection widget to Features tab

v0.4.3-alpha
- Changed plot borders to tighter layout
- Added option to export axial distribution data instead of just the plot

v0.4.2-alpha
- Changed "Volume/Area" in Misc Calculations widget to "Total Quantity" with selection drop-down
- Added length measurement to total quantity drop-down selection options

v0.4.1-alpha
- Added volume vs. area distinction for calculating domain diameter/radius in domain size distribution widget
- Fixed bug in calculating diameter and radius in domain size distribution widget (math is hard)

v0.4.0-alpha
- Added screenshot capture widget to I/O tab
- Added screenshot batch processing for same view of other 3D volumes
- Removed annoying file selection dialog background root window

v0.3.5-alpha
- Updated mask projection function name in all locations that was changed previously and not updated in all locations
- Fixed bug with automatic drop-down updating of label layers
- Fixed bug with painting on RGB images
- Added alternate x axis labeling to domain size distribution plot widget

v0.3.4-alpha
- Fixed domain size distribution plot to accept label layers instead of image layers

v0.3.3-alpha
- Fixed mask creation from shape coordinate rounding error
- Labeling widget now adds label layers with colors instead of grayscale image layer
- Label layers can be directly exported from I/O tab
- Label layers can be converted to image layers in Pixel Values tab
- Removed unnecessary duplicate code in widgets with mask application option

v0.3.2-alpha
- Added normalization method drop-down selection to axial distribution plot widget

v0.3.1-alpha
- Added call button label to skeleton detection widget

v0.3.0-alpha
- Added skeleton detection widget
- Fixed mask exporting but when exporting 2D mask with "compress masks" checked in parameters export

v0.2.0-alpha
- Completed readme file
- Created automatic expansion of 2D mask for 3D stack masking/cropping from 2D mask
- Fixed bug with inability to mask/crop 2D images
- Many minor changes to the default parameters in each widget

v0.1.0-alpha
- Initial release on GitHub
